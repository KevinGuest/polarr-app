version: "3.7"

services:
  app_proxy:
    environment:
      # Umbrel names containers: <app-id>_<service>_1
      APP_HOST: polarr-app_web_1
      APP_PORT: 3000
      PROXY_AUTH_WHITELIST: "/api/*"
      PROXY_AUTH_ADD: "false"

  web:
    # Digest pinned after GHCR publish — bump both on release.
    image: ghcr.io/kevinguest/polarr-app:0.5.0@sha256:ae4cac821f3bc425374ca5b52c603c35ea966a3dcda1e6171b601aa6e002381a
    user: "1000:1000"
    init: true
    restart: on-failure
    stop_grace_period: 30s
    environment:
      - POLARR_DATA_DIR=/data
      - POLARR_MUSIC_DIR=/music
      # Writable Polarr fallback dir — Lidarr complete/music is often not uid 1000
      - POLARR_DOWNLOADS_DIR=/data/downloads
      - PORT=3000
      - HOSTNAME=0.0.0.0
      - POLARR_APP_VERSION=0.6.0
    volumes:
      - ${APP_DATA_DIR}/data:/data
      # Same folder Lidarr uses: /downloads/complete/music
      - ${UMBREL_ROOT}/data/storage/downloads/complete/music:/music
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:3000/api/v1/status"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 25s
